import React from 'react';
import * as S from '../styles/loading.style';

const Loading = (props) => {
    return (
        <S.section>
            <p>loading</p>
        </S.section>
    )
}

export default Loading;